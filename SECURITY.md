# 安全策略

## 支持范围

安全修复面向当前 `main` 分支和最新正式版本。开发快照不承诺兼容性，但确认的安全问题仍会在当前开发分支修复。

## 部署边界

Rule Engine 当前不内置登录、JWT 验签或 RBAC。管理接口信任可信网关注入的 `X-Actor-Id` 和 `X-Actor-Type`，因此：

- 不要把管理接口直接暴露到公网；
- 在生产环境前置完成身份认证、权限校验、限流和 TLS 终止的网关；
- 不接受来自外部客户端自行构造的 actor 请求头；
- 数据库密码、连接器凭据、XXL-JOB Token 和观测系统密码必须由环境变量或密钥管理系统注入；
- trace 与审计数据可能包含业务敏感信息，应限制访问并配置适当保留期。

仓库中的 Docker Compose 仅用于本地开发，端口默认只绑定回环地址，不应直接作为生产部署模板。

## 报告漏洞

不要通过公开 Issue、Pull Request 或 Discussion 披露漏洞。仓库公开并启用 GitHub 私密漏洞报告后，请通过 Security Advisories 的私密报告入口提交，并尽量提供：

- 受影响版本或提交；
- 影响和可能的攻击路径；
- 最小复现步骤；
- 已知缓解措施或修复建议。

在私密报告入口启用前，请联系仓库所有者确认安全渠道，不要发送真实凭据、客户数据或未脱敏日志。
