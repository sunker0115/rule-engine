# v2 第二阶段实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补齐 v1 遗留的 API 契约缺口并完成 v2 阶段核心工程项，使引擎具备完整的前端集成能力与本地 E2E 开发闭环。

**Architecture:** 四个独立批次：(A) provided-metrics 发现 API（极小）；(B) node_trace 树重建（§6.2 契约缺口）；(C) docker-compose + 本地 E2E 启动；(D) §2.13 CompiledExecutor 预编译演进（性能）。批次 A→B→C 串行，D 可并行或放后续。

**Tech Stack:** Java 25 / Spring Boot 4 / MyBatis-Plus / Testcontainers / docker-compose

---

## 批次 A：§5.2 provided-metrics 发现 API

> `MetadataService.MetricMeta` 已有 `allowProvided` 字段，只需加一个 Controller 端点。

**Files:**
- Modify: `rule-api/src/main/java/com/sstlfsj/rule/web/config/MetadataController.java`
- Modify: `rule-config-svc/src/main/java/com/sstlfsj/rule/config/api/service/MetadataService.java`
- Modify: `rule-config-svc/src/main/java/com/sstlfsj/rule/config/internal/service/MetadataServiceImpl.java`
- Test: `rule-api/src/test/java/com/sstlfsj/rule/web/config/MetadataControllerTest.java`

### Task A-1: 加 `getProvidedMetrics` 端点

- [ ] **Step 1: 在 `MetadataService` 加 `ProvidedMetricsResponse` record + 方法签名**

```java
/** D30 provided-metrics 发现：返回 allowProvided=true 的指标列表。 */
record ProvidedMetricsResponse(java.util.List<MetricMeta> metrics) {}

/**
 * 返回指定场景中调用方可携带的指标列表（allowProvided=true）。
 *
 * @param tenantId  租户 ID
 * @param sceneCode 场景编码
 * @return 可被业务方随评估携带的指标元数据列表
 */
ProvidedMetricsResponse getProvidedMetrics(String tenantId, String sceneCode);
```

- [ ] **Step 2: 在 `MetadataServiceImpl` 实现**

```java
@Override
public ProvidedMetricsResponse getProvidedMetrics(String tenantId, String sceneCode) {
    // 复用 getSceneMetadata 相同的 scene 存在性校验 + metric 查询逻辑
    MetadataResponse all = getSceneMetadata(tenantId, sceneCode);
    List<MetricMeta> provided = all.availableMetrics().stream()
            .filter(MetricMeta::allowProvided)
            .toList();
    return new ProvidedMetricsResponse(provided);
}
```

- [ ] **Step 3: 在 `MetadataController` 加端点**

```java
/** GET /api/v1/scenes/{sceneCode}/provided-metrics — 发现 D30 allowProvided 指标 */
@GetMapping("/{sceneCode}/provided-metrics")
public ApiResponse<MetadataService.ProvidedMetricsResponse> getProvidedMetrics(
        @PathVariable String sceneCode,
        @RequestParam String tenantId) {
    return ApiResponse.ok(metadataService.getProvidedMetrics(tenantId, sceneCode));
}
```

- [ ] **Step 4: 写 `MetadataControllerTest`（MockMvc）**

找到已有 `MetadataControllerTest.java`，追加测试方法：

```java
@Test
void getProvidedMetrics_返回allowProvided为true的指标() throws Exception {
    MetadataService.MetricMeta allowedMetric = new MetadataService.MetricMeta(
            "user.kyc.level", "KYC等级", "LONG", "ATTRIBUTE", true);
    MetadataService.MetricMeta blockedMetric = new MetadataService.MetricMeta(
            "account.balance", "余额", "DECIMAL", "SQL_AGGREGATE", false);
    MetadataService.ProvidedMetricsResponse resp =
            new MetadataService.ProvidedMetricsResponse(List.of(allowedMetric));
    when(metadataService.getProvidedMetrics("100", "risk.transfer")).thenReturn(resp);

    mockMvc.perform(get("/api/v1/scenes/risk.transfer/provided-metrics")
                    .param("tenantId", "100"))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.data.metrics", hasSize(1)))
            .andExpect(jsonPath("$.data.metrics[0].metricCode").value("user.kyc.level"))
            .andExpect(jsonPath("$.data.metrics[0].allowProvided").value(true));
}

@Test
void getProvidedMetrics_无allowProvided指标时返回空列表() throws Exception {
    when(metadataService.getProvidedMetrics("100", "risk.transfer"))
            .thenReturn(new MetadataService.ProvidedMetricsResponse(List.of()));

    mockMvc.perform(get("/api/v1/scenes/risk.transfer/provided-metrics")
                    .param("tenantId", "100"))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.data.metrics", hasSize(0)));
}
```

- [ ] **Step 5: 运行 rule-api 测试**

```bash
export JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-25.jdk/Contents/Home
export PATH=$JAVA_HOME/bin:$PATH
MVN=/Users/sunke/.m2/wrapper/dists/apache-maven-3.9.9-bin/4nf9hui3q3djbarqar9g711ggc/apache-maven-3.9.9/bin/mvn
$MVN -pl rule-api,rule-config-svc -am test
```

Expected: BUILD SUCCESS

- [ ] **Step 6: 更新 `08-evolution.md` 加 §2.21 锚点、`10-api-contract.md §5.2` 标注已实装**

在 `08-evolution.md` §二 末尾追加：

```markdown
### 2.21 node_trace 树重建（来源 10-api-contract.md §6.2）

- **v1 现状**：`queryTrace` 返回扁平 `List<TraceNodeEntry>`，按 `node_path` 字典序排列；§6.2 契约要求返回嵌套树结构（与 §3.3 dry-run 的 `nodeTrace` 字段格式相同）。
- **触发条件**：前端 trace 可视化组件需要树状输入，扁平列表需前端重建，增加前后端耦合。
- **演进方向**：在 `AuditService` 新增 `queryTraceTree` 方法，按 `node_path`（点分数字，如 `"0.1.2"`）重建树；扁平 `queryTrace` 保留向后兼容。
- **迁移成本**：低（纯 API 层逻辑，不改存储）。
```

在 `10-api-contract.md §5.2` 标注：

```
> **已实装（v2 第二阶段）**
```

- [ ] **Step 7: commit**

```bash
git add -A && git commit -m "feat(metadata): 实装 §5.2 provided-metrics 发现 API"
```

---

## 批次 B：node_trace 树重建（§6.2）

> `node_trace` 落库是扁平行，`node_path` 为点分数字字符串（`"0"` `"0.0"` `"0.1"` `"0.1.0"`）。重建逻辑：按 `node_path` 深度优先，把子路径挂到父路径节点上。

**Files:**
- Modify: `rule-audit-svc/src/main/java/com/sstlfsj/rule/audit/api/service/AuditService.java`
- Modify: `rule-audit-svc/src/main/java/com/sstlfsj/rule/audit/internal/service/AuditServiceImpl.java`
- Modify: `rule-api/src/main/java/com/sstlfsj/rule/web/audit/AuditController.java`
- Test: `rule-audit-svc/src/test/java/com/sstlfsj/rule/audit/internal/service/AuditServiceImplTest.java`
- Test: `rule-api/src/test/java/com/sstlfsj/rule/web/audit/AuditControllerTest.java`

### Task B-1: `AuditService` 加树重建 record + 方法

- [ ] **Step 1: 在 `AuditService` 加 `TraceTreeNode` record 和 `queryTraceTree` 方法**

```java
/** 嵌套树节点，与 §3.3 dry-run nodeTrace 格式一致。 */
record TraceTreeNode(
        String nodeType,
        String conditionType,
        String metricCode,
        String actualValue,
        Boolean result,
        String errorCode,
        String valueSource,
        java.util.List<TraceTreeNode> children
) {}

/**
 * 查询指定评估会话的节点 trace 树（嵌套，与 §3.3 dry-run nodeTrace 格式一致）。
 *
 * <p>内部按 node_path（点分数字字符串）重建 AST 树结构：最短路径为根，子路径挂到父路径节点。</p>
 *
 * @param tenantId  租户标识
 * @param sessionId 评估会话 ID
 * @return 根节点列表（正常 AST 只有一个根，Pre-Gate 阻断时可能为空）
 */
java.util.List<TraceTreeNode> queryTraceTree(String tenantId, Long sessionId);
```

- [ ] **Step 2: 在 `AuditServiceImpl` 实现 `queryTraceTree`**

重建算法：先调 `queryTrace` 取扁平列表，再按 `node_path` 建树。

```java
@Override
public List<TraceTreeNode> queryTraceTree(String tenantId, Long sessionId) {
    List<TraceNodeEntry> flat = queryTrace(tenantId, sessionId);
    if (flat.isEmpty()) return List.of();
    // 按 node_path 长度排序，确保父节点先于子节点处理
    List<TraceNodeEntry> sorted = flat.stream()
            .sorted(java.util.Comparator.comparingInt(e -> e.nodePath().split("\\.", -1).length))
            .toList();
    // path -> 可变节点 builder（用 LinkedHashMap 保持 insertion 顺序）
    java.util.Map<String, TraceTreeNodeBuilder> byPath = new java.util.LinkedHashMap<>();
    List<String> roots = new java.util.ArrayList<>();

    for (TraceNodeEntry e : sorted) {
        TraceTreeNodeBuilder builder = new TraceTreeNodeBuilder(e);
        byPath.put(e.nodePath(), builder);
        String parent = parentPath(e.nodePath());
        if (parent == null) {
            roots.add(e.nodePath());
        } else {
            TraceTreeNodeBuilder parentBuilder = byPath.get(parent);
            if (parentBuilder != null) parentBuilder.children.add(builder);
        }
    }
    return roots.stream().map(r -> byPath.get(r).build()).toList();
}

/** 返回点分路径的父路径；根节点（不含 "."）返回 null。 */
private static String parentPath(String path) {
    int dot = path.lastIndexOf('.');
    return dot < 0 ? null : path.substring(0, dot);
}

/** 可变节点构建器，用于深度优先树重建。 */
private static final class TraceTreeNodeBuilder {
    final TraceNodeEntry entry;
    final List<TraceTreeNodeBuilder> children = new java.util.ArrayList<>();
    TraceTreeNodeBuilder(TraceNodeEntry e) { this.entry = e; }
    TraceTreeNode build() {
        return new TraceTreeNode(
                entry.nodeType(), entry.conditionType(), entry.metricCode(),
                entry.actualValue(), entry.result(), entry.errorCode(), entry.valueSource(),
                children.stream().map(TraceTreeNodeBuilder::build).toList()
        );
    }
}
```

- [ ] **Step 3: 在 `AuditServiceImplTest` 加树重建测试**

```java
@Test
void queryTraceTree_单层根节点() {
    NodeTraceRow root = new NodeTraceRow();
    root.setEvaluationSessionId(1L);
    root.setTenantId(100L);
    root.setNodePath("0");
    root.setNodeType("AndNode");
    root.setResult(true);

    when(nodeTraceMapper.selectList(any())).thenReturn(List.of(root));

    List<AuditService.TraceTreeNode> tree = service.queryTraceTree("100", 1L);

    assertThat(tree).hasSize(1);
    assertThat(tree.get(0).nodeType()).isEqualTo("AndNode");
    assertThat(tree.get(0).children()).isEmpty();
}

@Test
void queryTraceTree_父子关系正确重建() {
    NodeTraceRow rootRow = new NodeTraceRow();
    rootRow.setEvaluationSessionId(1L); rootRow.setTenantId(100L);
    rootRow.setNodePath("0"); rootRow.setNodeType("AndNode"); rootRow.setResult(true);

    NodeTraceRow childRow = new NodeTraceRow();
    childRow.setEvaluationSessionId(1L); childRow.setTenantId(100L);
    childRow.setNodePath("0.0"); childRow.setNodeType("ConditionNode");
    childRow.setConditionType("GT"); childRow.setMetricCode("user.age");
    childRow.setResult(true); childRow.setActualValue("25");

    when(nodeTraceMapper.selectList(any())).thenReturn(List.of(rootRow, childRow));

    List<AuditService.TraceTreeNode> tree = service.queryTraceTree("100", 1L);

    assertThat(tree).hasSize(1);
    AuditService.TraceTreeNode root = tree.get(0);
    assertThat(root.children()).hasSize(1);
    AuditService.TraceTreeNode child = root.children().get(0);
    assertThat(child.nodeType()).isEqualTo("ConditionNode");
    assertThat(child.metricCode()).isEqualTo("user.age");
    assertThat(child.actualValue()).isEqualTo("25");
}

@Test
void queryTraceTree_空rows返回空列表() {
    when(nodeTraceMapper.selectList(any())).thenReturn(List.of());
    assertThat(service.queryTraceTree("100", 999L)).isEmpty();
}
```

- [ ] **Step 4: 在 `AuditController` 加 `/trace/tree` 端点**

```java
/** GET /api/v1/evaluation-sessions/{sessionId}/trace/tree — 嵌套树格式（§6.2 完整契约） */
@GetMapping("/{sessionId}/trace/tree")
public ApiResponse<List<AuditService.TraceTreeNode>> getTraceTree(
        @PathVariable Long sessionId,
        @RequestParam String tenantId) {
    return ApiResponse.ok(auditService.queryTraceTree(tenantId, sessionId));
}
```

- [ ] **Step 5: 在 `AuditControllerTest` 补测试**

```java
@Test
void getTraceTree_返回嵌套结构() throws Exception {
    AuditService.TraceTreeNode child = new AuditService.TraceTreeNode(
            "ConditionNode", "GT", "user.age", "25", true, null, "FETCHED", List.of());
    AuditService.TraceTreeNode root = new AuditService.TraceTreeNode(
            "AndNode", null, null, null, true, null, null, List.of(child));
    when(auditService.queryTraceTree("100", 1L)).thenReturn(List.of(root));

    mockMvc.perform(get("/api/v1/evaluation-sessions/1/trace/tree")
                    .param("tenantId", "100"))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.data", hasSize(1)))
            .andExpect(jsonPath("$.data[0].nodeType").value("AndNode"))
            .andExpect(jsonPath("$.data[0].children", hasSize(1)))
            .andExpect(jsonPath("$.data[0].children[0].metricCode").value("user.age"));
}
```

- [ ] **Step 6: 运行测试**

```bash
$MVN -pl rule-audit-svc,rule-api -am test
```

Expected: BUILD SUCCESS

- [ ] **Step 7: 更新文档**

`10-api-contract.md §6.2` 追加：

```
**嵌套树端点（推荐）**：`GET /api/v1/evaluation-sessions/{sessionId}/trace/tree?tenantId=`  
返回格式与 §3.3 dry-run `nodeTrace` 相同（嵌套结构），已实装（v2 第二阶段）。  
扁平列表端点 `/trace`（无 `/tree`）保留向后兼容。
```

`08-evolution.md §2.21` 标注已实装。

- [ ] **Step 8: commit**

```bash
git add -A && git commit -m "feat(audit): §6.2 node_trace 树重建端点 /trace/tree"
```

---

## 批次 C：docker-compose + 本地 E2E 启动

> 目标：`docker-compose up` 后能 `curl /api/v1/rule/evaluate` 跑通完整链路，支持本地开发/手动验证，不依赖 IDEA。

**Files:**
- Create: `docker-compose.yml`（项目根）
- Modify: `rule-app/src/main/resources/application.yml`（环境变量化关键配置）
- Create: `docs/superpowers/plans/local-dev-guide.md`（启动说明，可选）

### Task C-1: docker-compose 基础

- [ ] **Step 1: 在项目根写 `docker-compose.yml`**

```yaml
version: "3.9"

services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: rule_engine
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-proot"]
      interval: 5s
      retries: 10

  rule-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/rule_engine?useSSL=false&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: root
    depends_on:
      mysql:
        condition: service_healthy
```

- [ ] **Step 2: 写 `Dockerfile`**

```dockerfile
FROM eclipse-temurin:25-jre
WORKDIR /app
COPY rule-app/target/rule-app-*.jar app.jar
ENTRYPOINT ["java", "-jar", "app.jar"]
```

- [ ] **Step 3: 确认 `application.yml` 使用环境变量覆盖**

检查 `rule-app/src/main/resources/application.yml`，datasource url/username/password 需支持 `${SPRING_DATASOURCE_URL:...}` 格式。若不支持，修改为：

```yaml
spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/rule_engine?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:root}
```

- [ ] **Step 4: 构建并验证**

```bash
export JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-25.jdk/Contents/Home
export PATH=$JAVA_HOME/bin:$PATH
MVN=/Users/sunke/.m2/wrapper/dists/apache-maven-3.9.9-bin/4nf9hui3q3djbarqar9g711ggc/apache-maven-3.9.9/bin/mvn
# 先打包，跳过测试（测试已在 CI 中跑）
$MVN -DskipTests package
# 启动 compose
docker-compose up -d
```

等待健康检查通过后：

```bash
# 验证启动
curl -s http://localhost:8080/actuator/health | grep -q '"status":"UP"' && echo "OK"
```

- [ ] **Step 5: 冒烟验证完整评估链路**

先建 Scene + Metric（或直接用 Flyway 初始化数据），再跑 PULL 评估：

```bash
curl -s -X POST http://localhost:8080/api/v1/rule/evaluate \
  -H "Content-Type: application/json" \
  -H "X-Actor-Id: smoke-test" \
  -d '{
    "tenantId": "1",
    "sceneCode": "risk.transfer",
    "eventType": "transfer.initiated",
    "subjectId": "user-001",
    "eventId": "smoke-001",
    "occurredAt": "2026-06-03T10:00:00Z",
    "payload": {}
  }'
```

Expected: HTTP 200，`ruleHit=false`（无规则命中，正常），`errorCode=null`。

- [ ] **Step 6: commit**

```bash
docker-compose down
git add docker-compose.yml Dockerfile rule-app/src/main/resources/application.yml
git commit -m "feat(ops): 添加 docker-compose + Dockerfile，支持本地 E2E 启动"
```

---

## 批次 D：§2.13 CompiledExecutor（性能演进）

> 前置：`RuleVersionExecutor` SPI + `rule_version.compiled_predicate_ref` 字段均已在 v1 预留。
> 触发条件：PUSH 模式 TPS 触及 InterpretedExecutor 瓶颈（5–10 μs/规则）。
> **此批次优先级低，可等真实性能数据后再启动。**

**Files:**
- Create: `rule-kernel/src/main/java/com/sstlfsj/rule/kernel/internal/evaluator/CompiledExecutor.java`
- Modify: `rule-eval-svc/src/main/java/com/sstlfsj/rule/eval/internal/EvalAutoConfiguration.java`（注册 ExecutorRegistry）
- Test: `rule-kernel/src/test/java/com/sstlfsj/rule/kernel/internal/evaluator/CompiledExecutorTest.java`

### Task D-1: CompiledExecutor 骨架 + Janino 编译

**依赖先决条件：** 在 `pom.xml` 加 Janino 依赖：

```xml
<!-- Janino：Java 运行时字节码编译，用于 AST 预编译 -->
<dependency>
    <groupId>org.codehaus.janino</groupId>
    <artifactId>janino</artifactId>
    <version>3.1.12</version>
</dependency>
```

在根 `pom.xml` `dependencyManagement` 中统一版本，`rule-kernel/pom.xml` 中引用。

- [ ] **Step 1: 写失败测试**

```java
@Test
void compiledExecutor_与InterpretedExecutor结果一致() {
    // 构造 AND(GT(user.age, 18)) 规则
    ConditionNode ageGt18 = new ConditionNode("GT", "user.age", Map.of("threshold", 18));
    AndNode root = new AndNode(List.of(ageGt18));
    RuleVersionSnapshot snap = /* 测试用 snap */;

    EvalContext ctx = /* user.age=25 */;

    InterpretedExecutor interpreted = new InterpretedExecutor(evaluators);
    CompiledExecutor compiled = new CompiledExecutor(evaluators);

    assertThat(compiled.execute(snap, ctx).satisfied())
            .isEqualTo(interpreted.execute(snap, ctx).satisfied());
}
```

- [ ] **Step 2: AST → Java 代码生成**

在 `CompiledExecutor` 实现 `compileToJava(AstNode root) → String`，把 AST 转成：

```java
// AndNode(GT(user.age,18), LT(user.vip,3)) 生成：
return ctx.metricValue("user.age") != null
    && ((Number)ctx.metricValue("user.age")).intValue() > 18
    && ctx.metricValue("user.vip") != null
    && ((Number)ctx.metricValue("user.vip")).intValue() < 3;
```

- [ ] **Step 3: Janino 编译 + `Predicate<EvalContext>` 缓存**

```java
// CompiledExecutor.execute() 核心流程
Predicate<EvalContext> predicate = cache.computeIfAbsent(
    snapshot.ruleVersionId(),
    id -> compile(snapshot.conditionAst())
);
boolean result = predicate.test(ctx);
return new EvalResult(result, ...);
```

缓存用 `ConcurrentHashMap<Long, Predicate<EvalContext>>`，`DISABLE` 事件触发 `cache.remove(versionId)`。

- [ ] **Step 4: 运行测试，确认通过**

```bash
$MVN -pl rule-kernel -am test
```

- [ ] **Step 5: `ExecutorRegistry` 支持灰度切换**

在 `EvalAutoConfiguration` 注册 `CompiledExecutor`，默认启用 `InterpretedExecutor`，通过配置项 `engine.executor.compiled.enabled=true` 切换：

```java
@Bean
@ConditionalOnProperty(name = "engine.executor.compiled.enabled", havingValue = "true")
public RuleVersionExecutor compiledExecutor(...) { ... }

@Bean
@ConditionalOnMissingBean(RuleVersionExecutor.class)
public RuleVersionExecutor interpretedExecutor(...) { ... }
```

- [ ] **Step 6: 更新 `08-evolution.md §2.13`** 标注已实装

- [ ] **Step 7: commit**

```bash
git add -A && git commit -m "feat(kernel): CompiledExecutor Janino 预编译实装（§2.13）"
```

---

## 验证命令汇总

```bash
export JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-25.jdk/Contents/Home
export PATH=$JAVA_HOME/bin:$PATH
MVN=/Users/sunke/.m2/wrapper/dists/apache-maven-3.9.9-bin/4nf9hui3q3djbarqar9g711ggc/apache-maven-3.9.9/bin/mvn

# 批次 A + B
$MVN -pl rule-config-svc,rule-audit-svc,rule-api -am test

# 批次 D
$MVN -pl rule-kernel -am test

# 全量
$MVN test
```
