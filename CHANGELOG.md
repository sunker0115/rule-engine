# 变更记录

本文件记录 Rule Engine 的对外变化，格式参考 Keep a Changelog，版本号遵循语义化版本。

## [未发布]

### 新增

- 增加 GPL-3.0-only 许可证、贡献指南、安全策略和社区行为准则。
- 增加默认 MySQL 配置的临时数据库启动验证，以及 Docker 临时 MySQL 镜像烟测。

### 变更

- 将公开版本前的 43 个开发期 Flyway migration 收敛为单一最终结构基线。
- 默认运行、集成测试和生产部署统一使用 MySQL；默认启动无需指定 profile，连接信息可由数据源环境变量覆盖，`mysql` profile 保留严格部署凭据要求。
- Docker Compose 的密码改由本地 `.env` 注入，端口仅绑定回环地址，并关闭 Grafana 匿名管理员访问。

### 安全

- 明确管理接口依赖可信鉴权网关，禁止直接暴露到公网。
- `mysql` profile 要求显式配置数据库连接信息；移除 XXL-JOB 弱默认凭据。

## [0.7.1] - 2026-07-23

### 新增

- 支持基于 Virtual Thread 的并行规则求值。
- 增加六种表达式引擎通用的变量补全、CEL 类型诊断，以及表达式实时校验接口。
- 增加参考项目与业界选型对比台账。

### 修复

- 修正负载测试的指标传递条件与调度模块集成测试中的过时列名。

### 性能

- 50 条规则顺序求值达到 19k req/s，p95 为 19.9ms；取数缓存命中达到 30k req/s，p95 为 15.9ms；缓存穿透的冷路径约为 3k req/s，p95 为 117.5ms。

## [0.7.0] - 2026-07-23

### 新增

- 增加 `DECISION_FLOW` 规则种类、多态规则体与 Flow 画布编辑器，支持拖放连线和 RuleRef 分支路由。
- 增加画布节点状态警告、试算路径高亮、What-if 自动重算，以及快捷保存、撤销/重做、PNG 和 RuleBundle/Snapshot 导出。
- 增加 SDK 从导出快照加载规则的端到端验证。

### 变更

- 前端调整为可折叠的三栏自适应布局。

## [0.6.2.1] - 2026-06-23

### 变更

- 统一错误响应为 RFC 9457 `ProblemDetail`，并补充请求地址日志。

## [0.6.2] - 2026-06-20

### 新增

- 完成调度子系统：Scheduler SPI 支持调度与触发广播，XXL-JOB 支持分片广播及 config 到 eval 的跨实例传播。

### 变更

- 调度后端默认使用进程内实现；`xxl-job` 改为按需启用。

## [0.6.1] - 2026-06-19

### 新增

- 增加规则与指标的双向血缘查询，以及指标停用拦截。
- 增加规则编辑器版本生命周期闭环，支持新版本克隆、丢弃草稿和删除规则。

### 变更

- 保存规则后自动刷新规则集分析，统一前端分析入口与中栏标题。

## [0.6.0] - 2026-06-18

### 新增

- 增加规则集静态分析与冲突检测，覆盖不一致、死规则、冲突、重叠、覆盖缺口和冗余等场景。
- 增加规则集分析 API 与前端摘要、抽屉和徽标入口。

### 修复

- 消除指标定义热更新时的读取空洞。

## [0.5.1] - 2026-06-14

### 变更

- 将六种表达式引擎及其 Starter 归入 `rule-expression` 聚合模块，并将 WireMock 版本集中管理；无对外行为变更。

## [0.5.0] - 2026-06-14

### 新增

- 发布开发预览版本：配置写入、规则评估、审计落库与脱敏链路完成真实服务和真实数据库端到端验证。
- 支持 PULL、PUSH、HYBRID 求值模式、dry-run 与 replay 复现。
- 支持 AST 与 `EXPRESSION_SCRIPT` 规则，以及 CEL、Aviator、QLExpress、JsonLogic、JEXL、Groovy 六种表达式引擎。

### 变更

- 升级至 Spring Boot 4.1、Spring Modulith 2.1 和 Java 25。
