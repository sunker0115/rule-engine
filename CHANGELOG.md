# 变更记录

本文件记录 Rule Engine 的对外变化，格式参考 Keep a Changelog，版本号遵循语义化版本。

## [未发布]

### 新增

- 增加 GPL-3.0-only 许可证、贡献指南、安全策略和社区行为准则。
- 增加默认 MySQL 配置的临时数据库启动验证，以及 Docker 临时 MySQL 镜像烟测。

### 变更

- 将公开版本前的 43 个开发期 Flyway migration 收敛为单一最终结构基线。
- 默认运行、集成测试和生产部署统一使用 MySQL；默认启动无需指定 profile，连接信息可由数据源环境变量覆盖，`mysql` profile 保留严格部署凭据要求。
- Docker Compose 的密码改由本地 `.env` 注入，端口仅绑定回环地址，并关闭 Grafana 匿名管理员访问。

### 安全

- 明确管理接口依赖可信鉴权网关，禁止直接暴露到公网。
- `mysql` profile 要求显式配置数据库连接信息；移除 XXL-JOB 弱默认凭据。

## [0.7.1] - 2026-07-23

### 新增

- 支持基于 Virtual Thread 的并行规则求值。
- 增加 CEL 表达式的变量补全与类型诊断，以及表达式实时校验接口。
- 增加参考项目与业界选型对比台账。

### 修复

- 修正负载测试的指标传递条件与调度模块集成测试中的过时列名。
